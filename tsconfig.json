{
  "compilerOptions": {
    /* =========================
     * 🎯 编译目标 & 语言特性
     * ========================= */
    "target": "ES2020", // 生成的 JS 目标版本（影响 async/await 等语法）
    "lib": [
      "ES2020", // ES 标准库（Promise、Map、Set 等）
      "DOM", // 浏览器 DOM API
      "DOM.Iterable" // 可迭代的 DOM 集合（如 NodeList）
    ],
    /* =========================
     * 🧩 装饰器相关（关键配置）
     * ========================= */
    "experimentalDecorators": true, // 启用装饰器语法（@property）
    "emitDecoratorMetadata": false, // 是否生成运行时反射元数据（一般不用就关）
    "useDefineForClassFields": false, // 使用旧的 class field 行为（装饰器必须）
    /* =========================
     * 📦 模块系统 & 打包器模式
     * ========================= */
    "module": "ESNext", // 保留 ES Module 语法，交给 Vite 处理
    "moduleResolution": "bundler", // 使用适配打包器的模块解析策略
    "allowImportingTsExtensions": true, // 允许 import './xxx.ts'
    "resolveJsonModule": true, // 允许 import json 文件
    /* =========================
     * ⚡ Vite / 构建性能优化
     * ========================= */
    "isolatedModules": true, // 每个文件可独立编译（esbuild / swc 必需）
    "noEmit": true, // TS 不生成 JS，只做类型检查
    "skipLibCheck": true, // 跳过 node_modules 类型检查，加快速度
    /* =========================
     * ⚛️ React & JSX
     * ========================= */
    "jsx": "react-jsx", // React 17+ 新 JSX 运行时
    /* =========================
     * 🧪 类型检查 & 代码质量
     * ========================= */
    "strict": true, // 启用严格模式（推荐）
    "noUnusedLocals": false, // 不报未使用的局部变量
    "noUnusedParameters": false, // 不报未使用的参数
    "noFallthroughCasesInSwitch": false // switch case 允许贯穿
  },
  /* =========================
   * 📁 项目文件范围
   * ========================= */
  "include": [
    "src" // 只检查 src 目录下的文件
  ],
  /* =========================
   * 🧱 多 tsconfig 项目引用
   * ========================= */
  "references": [
    {
      "path": "./tsconfig.node.json" // Node / Vite 配置用的 tsconfig
    }
  ]
}